This folder contains thread-level (conversation block) mappings.

Each file represents grouped tweet conversations processed from the dataset.

Data is stored in:
- JSON format for structured programmatic use
- XLSX format for easy inspection and analysis

These mappings are generated after constructing the tweet dependency graph and resolving execution order using topological sorting.